# W.I.T.C.H. Tetris — Nintendo 3DS: паспорт продолжения

Этот файл нужно передать в новый чат вместе со ссылкой на репозиторий. Он
фиксирует состояние нативного порта после аппаратного отчёта 16.09.2026.

## Репозиторий и проверенная сборка

- Репозиторий: `Arrrtemka246/Witch_tetris_3ds`.
- Рабочая ветка 3DS: `chatgpt/3ds-fixes-rc1`.
- Проверенный commit: `bd9888bfcc9ed145d3d41b6812937259aef99aa9`.
- Главный исходник: `native3ds/source/main.cpp`.
- Workflow: `.github/workflows/build-native-rc2-fast.yml`.
- Успешный GitHub Actions run: `35086402338`.
- CIA SHA-256: `5abb788356c55f61c7d1f82d0b41c5e88eff3a2cef5af8baf7ce85df83696146`.

Артефакты run:

- `witch-tetris-3ds-rc3-native`: 3DSX, map, SMDH и исходник.
- `witch-tetris-3ds-rc3-final`: CIA и файл SHA-256.

## Что исправлено в Story Test 2

- Исправлена причина белых силуэтов: `Art::drawFit()` больше не заменяет RGB
  изображения белым Citro2D tint с коэффициентом 1.0.
- Подключены настоящие атласы `phase1_cells.t3x` и `horror_cells.t3x` — 112
  фрагментов фигур вместо одноцветных прямоугольников.
- `NEW GAME` сразу начинает Tetris. Выбор Стражницы/Фобос появляется только
  после 200 линий и предшествующей кат-сцены.
- Возвращены сюжетные рубежи 100 и 200 линий. Intro, 100 и 200 состоят из
  нескольких кадров; в меню `CUTSCENES` они доступны отдельно.
- Выбор после 200 меняет музыку и фигуры: Guardian route — обычные блоки,
  Phobos route — horror-атлас с вероятностью 80%.
- После проигрыша до выбора показывается управляемый `GAME OVER` с пунктами
  `RESTART` и `MAIN MENU` на обоих экранах.
- После проигрыша на Guardian route показывается семикадровая концовка.
  На Phobos route открывается Phobos Room; обычного выхода в меню там нет.
- Пауза имеет выбираемые пункты на нижнем экране и управление касанием.
- На нижнем экране игры есть кнопка `KEYBOARD / CODES` (`KB` в двухэкранном
  режиме). На экране `WHO WINS?` есть отдельная большая сенсорная кнопка.
- `Q` добавляет 10 линий и запускает сюжетные пороги, поэтому до 200 можно
  быстро дойти во время теста.
- `WITCH`, `VICH`, `ВИТЧ`, `ВИЧ` очищают стакан во время партии.
- `PHOBOS/ФОБОС` вызывает реакцию во время партии; на `WHO WINS?` выбирает
  Phobos route.
- `VTD`, `MATRIX`, `JTX/JETIX`, `GPT/CHATGPT`, `SUNO/SUNA`, `PORN`
  разведены в отдельные эффекты.
- Matrix проигрывает 214 кадров (6 fps, исходные 35,7 секунды) с аудио и затем
  закрывает приложение. VTD закрывает приложение по окончании трека.
- `PORN` на `WHO WINS?` проигрывает 3,1-секундную заставку и возвращается к
  выбору. Во время партии он сохраняет случайную безопасную карточку и реплику
  Фобоса.
- GPT и Suno показывают локальные офлайн-карточки с favicon; браузер 3DS не
  открывается.
- Музыка каждой фазы: фиксированный первый трек, затем shuffle-bag без
  предсказуемого последовательного обхода.
- A+B переключает компактный/двухэкранный стакан 10+10 и сохраняет настройку.

## Как воспроизвести сборку

Workflow сам:

1. Получает корневой `witch_tetris_3ds.3dsx` через Git LFS.
2. Извлекает его RomFS.
3. Собирает статический mpg123 для ARM.
4. Готовит офлайн-логотипы и T3X-кадры двух видео.
5. Собирает нативный 3DSX.
6. Упаковывает CIA через cxitool и makerom.

Локально нужны devkitPro/devkitARM, libctru, Citro2D, tex3ds, ImageMagick,
FFmpeg и статическая ARM-сборка mpg123.

## Приоритет аппаратного теста

1. Убедиться, что Фобос и все персонажи цветные, не белые силуэты.
2. Проверить, что фигуры состоят из фрагментов персонажей и корректно
   поворачиваются/остаются целыми после очистки линий.
3. `NEW GAME` -> сразу партия; 10×`Q` -> сцена 100; ещё 10×`Q` -> сцена
   200 -> `WHO WINS?`.
4. Проверить обе ветки, Game Over, паузу, сенсорные пункты и Phobos Room.
5. На `WHO WINS?` проверить Matrix, VTD, JTX, GPT, Suno и Porn.
6. Проверить compact/dual переключением A+B, особенно границу экранов.

В отчёте всегда указать модель 3DS, формат запуска (`.3dsx` или `.cia`),
экран, нажатия, ожидаемый и фактический результат; приложить фото/видео.

## Что ещё не считается законченным

- Нужен реальный аппаратный тест Story Test 2: CI проверяет сборку, но не GPU,
  скорость загрузки кадров и конкретную прошивку консоли.
- Нативные кат-сцены используют оригинальные арты, но пока являются компактной
  адаптацией, а не покадровым переносом всех таймингов 6210-строчного `main.py`.
- Семикадровая Guardian ending — рабочая адаптация; полный мультик из Python
  ещё предстоит перенести покадрово.
- Расширенные цепочки реплик и эмоций Phobos Room перенесены не полностью.
- CIA banner с настоящей 3D-моделью/параллаксом и звуковой реакцией пока не
  сделан; текущая SMDH-иконка сохранена.
- Мини-игры и часть коллекции Python-версии не входят в этот тестовый порт.

## Правило следующих итераций

После каждой правки выпускать оба формата (`.3dsx` и `.cia`), сохранять
commit SHA, Actions run URL и SHA-256 CIA. Не называть сборку полной до
аппаратного теста всех затронутых экранов.
