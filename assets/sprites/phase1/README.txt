PHASE I CHARACTER SPRITES — prototype build

Mapping locked for this project:
I = Cornelia
T = Caleb
O = Blunk
L = Hay Lin
J = Taranee
S = Irma
Z = Will

phase1_atlas.png + phase1_atlas.json are generated game assets.
The *_source files are the current source/reference images supplied in chat.
Caleb currently comes from the supplied screenshot, so it is intentionally a prototype-quality crop.

The engine stores each of the four visual fragments independently after a piece locks.
Therefore line clears can remove only part of a transformed character. This is intentional.
The falling character image rotates with the tetromino, including sideways/upside-down orientations.
